/** Detail payload for `form-layout-next`, `form-layout-back`, and `form-layout-submit` events. */
export interface FormLayoutStageEventDetail {
  /** The stage index after the navigation action (0-based). */
  stage: number
}
