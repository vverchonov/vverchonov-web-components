/** Allowed HTML input types for `<app-input>`. */
export type InputType = 'text' | 'email' | 'password' | 'number' | 'tel' | 'url' | 'search'

/** Visual validation state for `<app-input>`. */
export type InputState = 'default' | 'valid' | 'invalid'
