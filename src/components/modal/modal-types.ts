/** Button-layout variant for `<app-modal>`: info (OK), confirm (Cancel + Confirm), form (Cancel + Save). */
export type ModalVariant = 'info' | 'confirm' | 'form'

/** Dialog width preset for `<app-modal>`. */
export type ModalSize = 'sm' | 'md' | 'lg'
